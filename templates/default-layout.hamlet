<ul .breadcrumb>
    $forall node <- h
      <li>
        <a href="@{fst node}">#{snd node}
        \ 
        <span .divider>/
        \ 

    <li .active>#{t}

$maybe msg <- mmsg
  <div .alert-message.info .fade .in data-alert="alert">
    <a .close href="#">×
    <p>#{msg}

<div #login>
  <ul>
    $maybe u <-muid
      <li .dropdown data-dropdown="dropdown">
        <a href="#" .dropdown-toggle>
          $maybe grav <- mgrav
            <img src="#{grav}" .icon alt="gravatar">

          #{fromMaybe "anonymous" $ userName $ snd u}

        <ul .dropdown-menu>
          <li>
            ^{link ProfileR}
          <li>
            ^{link $ CommentsAdminR OverviewR}

          $if userAdmin $ snd u
            <li .divider>
            <li>
              ^{link ManagePostsR}

          <li .divider>
          <li>
            ^{link $ AuthR LogoutR}

    $nothing
      <li .dropdown>^{link $ AuthR LoginR}

^{widget}
